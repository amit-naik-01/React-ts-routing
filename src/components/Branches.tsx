import React from "react";

const Branches =() => {
    return(
        <div>
            <h2 style={{display:'flex',justifyContent:"center"}}>Branches</h2>
        </div>
    )
}

export default Branches;
import React from "react";

const Pipelines =() => {
    return(
        <div>
          <h2 style={{display:'flex',justifyContent:"center"}}>Pipelines</h2>
        </div>
    )
}

export default Pipelines;